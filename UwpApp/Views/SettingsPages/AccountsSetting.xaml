<Page
    x:Class="UwpApp.Views.SettingsPages.AccountsSetting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpApp.Views.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Left" MaxWidth="600" MinWidth="400">
            <TextBlock Text="Add Account" Style="{StaticResource HeaderTextBlockStyle}"></TextBlock>

            <TextBox Width="400" Header="Account Id" Text="{x:Bind viewModel.id}" PlaceholderText="Account Id"> </TextBox>
            <TextBox Width="400" Header="Account Name" Text="{x:Bind viewModel.AccountName}" PlaceholderText="Account Name"></TextBox>
            <TextBox Width="400" Header="Account Code" Text="{x:Bind viewModel.AccountCode}"></TextBox>


        </StackPanel>
        

    </Grid>
</Page>
